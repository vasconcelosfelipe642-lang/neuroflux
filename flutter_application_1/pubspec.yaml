name: flutter_application_1
description: NeuroFlux - Pequenas etapas, grandes conquistas.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1          # chamadas HTTP à API REST
  shared_preferences: ^2.3.3
  flutter_animate: ^4.5.2
  confetti: ^0.8.0
  audioplayers: ^6.0.0
  lottie: ^3.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/animations/
    # Descomente quando assets/sounds/complete.mp3 existir:
    # - assets/sounds/
